{
  "builders": [{
    "type": "docker",
    "image": "orlin/ubuntu-ansible:1.5.3",
    "export_path": "packer/web-docker.tar"
  }],
  "provisioners": [{
    "type": "ansible-local",
    "playbook_file": "web.yaml",
    "staging_directory": "packer/tmp/ansible"
  }]
}
